name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun test
      - run: bun run build
      - name: Smoke test the bundle over stdin
        run: cat dist/bots.ts | bun run - list
      - name: "Smoke test install from a repo tarball (what bunx github: does)"
        run: bun run smoke:tarball
