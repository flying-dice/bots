{
  "name": "@flying-dice/bots",
  "version": "0.1.0",
  "description": "One agent roster with configurable variants for Claude Code and Codex.",
  "type": "module",
  "bin": {
    "bots": "./src/bin.ts"
  },
  "files": [
    "src",
    "roles",
    "variants",
    "skills",
    "README.md"
  ],
  "scripts": {
    "start": "bun src/bin.ts",
    "build": "bun scripts/build.ts",
    "release": "bun scripts/release.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "smoke:tarball": "bun scripts/smoke-tarball.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/flying-dice/bots.git"
  }
}
